
let app = require("../app.js");
app.controller('PublicController', ['$state', '$rootScope', '$scope',
    function ($state, $rootScope, $scope) {
    debugger
        $rootScope.super_go = function (_view, _data, _option) {

        };

    }
]);